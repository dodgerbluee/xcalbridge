services:
  xcalbridge:
    build: .
    container_name: xcalbridge
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
      - SYNC_INTERVAL_HOURS=3
    restart: unless-stopped
